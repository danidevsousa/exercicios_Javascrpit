// Dados primitivos: String, number, undefined, null, boolean, symbol
const nome  = 'Danielle';//String
const nome1 ="Danielle";//String
const nome2 =`Danielle`;//String
const num1 = 1;//Number
const num2 = 2.5;//Number
let nomeAluno;// undefined = não aponta pra local nenhuma na memória.
let sobrenomeAluno = null; //Nulo -> não aponta pra local nenhuma na memória.
const aprovado = true; //Booelan = true, false (lógico).


let a = 2;  
const b = a;
console.log(a ,b);
a = 3;
console.log(a ,b);


